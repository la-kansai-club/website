# Sanityプロジェクト作成手順（初回のみ）

このガイドは、LA Kansai Clubのサイトを動かすために必要な「Sanity」というサービスのアカウント・プロジェクトを、初めて作る方向けにまとめたものです。専門的な操作は出てきません。ブラウザの画面を順番にクリックしていくだけで完了します。

**始める前に**：`OPERATIONS.md`で決めたとおり、クラブ専用の管理用Googleアカウント（作成済み。実際のアドレスは非公開の引き継ぎ資料を参照）でログインできる状態にしておいてください。**個人のGoogleアカウントではなく、必ずこのクラブ専用アカウントを使ってください。** 個人アカウントで作ってしまうと、その方が抜けたときにサイトの管理ができなくなってしまいます。

---

## 1. Sanityプロジェクトの作成手順

1. ブラウザで [https://www.sanity.io/](https://www.sanity.io/) を開きます
2. 画面右上あたりにある「Get started」「Sign up」のようなボタンをクリックします（ボタンの文言は今後変わる可能性があります。「登録」「無料で始める」のような意味合いのボタンを探してください）
3. ログイン方法の選択肢が表示されるので、**「Continue with Google」**（Googleで続ける）を選びます
4. Googleのログイン画面が出るので、**クラブ専用の管理用Googleアカウント**でログインします（表示されたアカウント一覧に個人アカウントも出てくる場合があるので、選び間違えないよう注意してください）
5. 初回だけ「何を作りますか？」のような簡単な質問が表示されることがあります。今回はすでに完成したコードがあるので、**「スキップ」または一番シンプルな選択肢**を選んで進めてください
6. 「組織（Organization）」の作成を求められることがあります。名前を聞かれたら、個人名ではなく **「LA Kansai Club」** のように、クラブだとひと目でわかる名前にしてください
7. 「新しいプロジェクトを作成」という画面になったら、次の「3. 推奨する設定値」を見ながら入力してください
8. 「テンプレートを選ぶ」という画面が出た場合は、必ず **「Empty project」（空のプロジェクト）** を選んでください。**サンプル入りのテンプレートは選ばないでください。** イベント・お知らせ・ギャラリー用の設計はすでに私たちの方でコードとして用意してあるため、テンプレートを選んでしまうと内容が重複・混在してしまいます
9. プロジェクトが作成されたら、この手順は完了です

---

## 2. Googleアカウントでログインした後の選び方（まとめ）

| 聞かれること | 選ぶもの |
|---|---|
| ログイン方法 | Continue with Google |
| どのGoogleアカウントを使うか | クラブ専用の管理用アカウント（個人アカウントは選ばない） |
| 作ろうとしているものの質問 | スキップ、または一番シンプルな選択肢 |
| 組織(Organization)の名前 | 「LA Kansai Club」など、クラブ名だとわかるもの |
| プロジェクトのテンプレート | 「Empty project」（空のプロジェクト） |

---

## 3. 推奨する設定値

| 項目 | 推奨値 | 理由 |
|---|---|---|
| Project Name（プロジェクト名） | `LA Kansai Club` | 管理画面上の表示名です。分かりやすければ他の名前でも問題ありません |
| Dataset Name（データセット名） | `production` | コード側がこの名前を前提にしているため、**必ずこの通り、小文字・スペースなしで入力してください** |
| Organization（組織） | `LA Kansai Club` | 個人名にしないことが重要です（OPERATIONS.md参照） |

---

## 4. `.env.local` に設定する値の一覧（取得場所の説明）

`.env.local`というファイルにこれらの値を設定する必要がありますが、**このファイル自体は、あなたから必要な値を教えていただいた後に私が作成します。** ここでは「どこを見ればその値が分かるか」だけ説明します。

| 設定名 | どこで確認できるか |
|---|---|
| `NEXT_PUBLIC_SANITY_PROJECT_ID` | プロジェクト作成後の画面、または管理画面の「Project settings」（歯車アイコン）に表示される「Project ID」という文字列。英数字の短い文字列です |
| `NEXT_PUBLIC_SANITY_DATASET` | 3.で決めた通り `production` |
| `NEXT_PUBLIC_SANITY_API_VERSION` | 調べる必要はありません。すでにコード側に用意済みの値をそのまま使います |
| `SANITY_API_TOKEN` | 今回は使いません。将来必要になった時点で改めてご案内します |

**お願いしたいこと**：プロジェクト作成が終わったら、上記の「Project ID」の文字列だけをお知らせください。それ以外の個人情報やパスワードを共有いただく必要は一切ありません。

---

## 5. 接続後の動作確認チェックリスト

Project IDを共有いただき、私が接続設定を行った後に、一緒に次の内容を確認します（本番データはまだ作りません。確認用の仮データはテスト後に削除します）。

- [ ] `npm run dev`でサイトを起動し、エラーが出ずに立ち上がる
- [ ] ブラウザで `http://localhost:3000/studio` を開き、Sanityのログイン画面が表示される
- [ ] クラブ専用の管理用Googleアカウントでログインでき、管理画面が開く
- [ ] 管理画面の左側に「イベント」「お知らせ」「ギャラリーアルバム」の3つの項目が表示されている
- [ ] 試しに「イベント」を1件、仮のタイトル（例：「テスト投稿」）で作成し、Publishする
- [ ] トップページ（`http://localhost:3000`）を開き、「次のイベント」に今作った仮のイベントが表示される（反映まで1分程度かかる場合があります）
- [ ] Hero・About紹介・Join紹介・Footerなど、Sanityと関係ない部分がこれまで通り表示されている
- [ ] 確認できたら、テストで作成したイベントをSanityの管理画面から削除する

---

## 実装について

このガイドはあくまで準備・手順の共有です。**実際にサイトのコードへ接続設定を反映する作業は、あなたから「設定が完了しました」とご連絡いただいてから着手します。** それまでコード側の変更は行いません。
