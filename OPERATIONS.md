# 運用方針

このサイトは
「役員がコードを書かずに更新できること」
を最優先とします。

技術的な高度さよりも、
長期間・安全・簡単に運営できることを優先します。

特定の個人ではなく、
LA関西クラブの資産として管理します。

---

# 管理体制の基本

Webサイト管理専用のGoogleアカウントを1つ新規作成し、このアカウントを使ってサイト運営に関わる**すべてのサービスを一元管理**します。

**管理アカウント**：作成済み。実際のメールアドレスは、このリポジトリには記載せず、非公開の引き継ぎ資料（管理アカウントのGoogle Drive内）にのみ記録します

このアカウントで一元管理するサービス：

- Sanity（コンテンツ管理・CMS）
- GitHub（コードの保管場所）
- Vercel（サイトの公開・ホスティング）
- Google Analytics（アクセス解析）
- Google Search Console（検索結果への表示状況の確認）
- Google Drive（運用マニュアル・引き継ぎ文書の保管）
- その他、今後サイト運営に必要となるサービス

**このアカウントは一般公開しません。** Contactページやサイト上の連絡先には使わず、SNSにも登録しません。あくまで「サイト運営の元栓」として温存し、日常のログインには使いません。

日々の更新作業（Sanityでの記事編集など）は、理事一人ひとりが自分自身のメールアドレスで各サービスに招待され、そのアカウントで行います。管理アカウントのパスワードを日常的に共有・入力する必要はありません。

---

## 1. Googleアカウントは誰のものを使うべきか／団体アカウントを作るべきか

**特定の理事の個人Gmailは使わず、団体専用の新規アカウントを作成します。**

- 現職の会長や担当理事の個人アカウントを使うと、その方が引退・引っ越し・音信不通になった瞬間にサイト全体へのアクセスを失うリスクがあります
- この管理アカウントをSanityの「Organization（組織）」・GitHubの「Organization」・Vercelの「Team」など、各サービスの団体単位の機能の所有者として登録します。個人の契約ではなく団体の契約として扱うことで、特定個人への依存をなくします
- このアカウント自体には2段階認証を必ず設定します（3.参照）

## 2. 理事交代時の引き継ぎ方法

引き継ぎ手順を文書化し、管理アカウントのGoogle Drive内（マニュアル置き場、パスワードは置かない）に保管し、現職理事全員がアクセスできる状態にします。

**引き継ぎ時に行う3ステップ**
1. 新しい担当理事のメールアドレスを、各サービス（Sanity・必要であればVercel等）に編集者として招待する
2. 新担当者が実際にログインでき、試しに記事を1件編集・公開できることを確認する
3. 確認が取れたら、前任者のアカウントを各サービスの一覧から削除する

**年1回の点検**
総会など役員が揃うタイミングで、各サービスに現在アクセス権を持つ人の一覧を見直します。辞めた方のアクセスが残ったままにならないようにするための定期チェックです。管理アカウント自体のパスワードも、理事交代のタイミングで更新します。

## 3. パスワード管理

**管理アカウントのパスワードはGoogle Driveなど、サイト自体からアクセスできる場所には保存しません。** サイトの管理アカウント自体が乗っ取られた場合に、パスワードごと流出することを防ぐためです。代わりに、以下のいずれかで管理します。

**推奨：Bitwarden（パスワード管理サービス）**
- 複数人で安全にパスワードを共有できる「組織（Organization）」機能があり、メンバーの追加・削除も簡単です
- 低コスト（年間数千円程度）で運用でき、理事交代時のパスワード更新や共有もアプリ上で完結します
- 会長・副会長・Web担当など、必要な理事だけがアクセスできる共有vaultを作成します

**代替案：紙に書いて封印し、会長・副会長が保管**
- 管理アカウントのID・パスワードを紙に書き、封筒に入れて封をします
- 封筒は2部作成し、会長・副会長がそれぞれ1部ずつ保管します（片方が紛失してももう片方で復旧可能）
- 実際に開封した場合は、開封後すぐにパスワードを変更し、新しい紙を用意して再封印します（封が切られている＝過去に使用された、と分かる状態を保つため）

小規模な団体であれば、まずは「紙で封印」だけでも十分機能します。編集者が増えてきた場合はBitwardenへの移行を検討してください。

## 4. 編集者の追加・削除方法

日常の編集作業は、管理アカウントではなく**各理事自身のメールアドレス**をSanityに招待する形で行います。

- Sanityの管理画面（ブラウザ）の「Members（メンバー）」から、メールアドレスを入力して招待するだけで追加できます
- 削除も同じ画面からワンクリックで可能です
- **権限は2段階に分けます**
  - **Administrator（管理者）**：メンバーの追加・削除ができる。会長・副会長など1〜2名のみに限定
  - **Editor（編集者）**：記事の作成・編集・公開ができるが、他の人を追加・削除する権限は持たない。実際に更新作業をする理事に付与
- 管理アカウント自体へのログイン権限（GitHub・Vercelなどの土台部分）は、上記のSanity編集者権限とは別に、Administrator相当の理事のみに限定します

## 5. 誤って記事を削除した場合の復旧方法

- Sanityの各記事には「履歴（History）」機能があり、過去の状態を編集者自身がブラウザ上で見て元に戻せます。開発者を呼ぶ必要はありません
- 無料プランでは履歴を遡れる期間に限りがあるため、契約時に最新の条件を確認します
- 追加の安全策として、**年1回程度、開発者にデータ全体のバックアップ書き出しを依頼する**ことを推奨します（8.バックアップ方法を参照）

## 6. 写真容量が増えた場合の運用

- Sanityの無料プランには写真などのアップロード容量に上限があります
- 対応策（コストがかからない順）
  1. 古いイベントアルバムのうち、特に思い出深い写真だけを残し、その他は間引く
  2. アップロードする写真のサイズを大きくしすぎない
  3. それでも足りなくなった場合は、有料プランへの切り替えを検討する
- 容量が増えるのはクラブの活動が長く続いている証であり、想定内の成長として捉えて問題ありません

## 7. 無料プランの上限に達した場合の対応

- Administrator権限を持つ理事は、Sanityの管理画面から現在の使用量をいつでもブラウザで確認できます。年1回の点検（2.）のタイミングで、あわせて使用量も確認します
- 上限に近づいた場合：①6.の方法で整理できないか確認 → ②最も低い有料プランへの切り替えを検討 → ③NPO向け割引の有無を確認、の順で対応します

---

# 8. 開発者向け情報

将来、外部の開発者に依頼する場合や、新しい担当者がコードに触れる場合のための技術情報です。日常の記事更新にはこの章の知識は不要です。

**使用技術**
- Next.js 14（App Router）+ TypeScript
- Tailwind CSS 3.4（デザイントークンは`website/tailwind.config.ts`に集約。値は`DESIGN_RULES.md`に準拠）
- Sanity（ヘッドレスCMS）。スキーマ・接続コードは実装済み。Sanityプロジェクトは作成済み（Project ID: `gpi6bjc8` / Dataset: `production`）
- Node.js：20 LTS以降を推奨（開発時はNode 24で動作確認済み）

**リポジトリ・デプロイ**
- コードはGitHub（Organization: `la-kansai-club` / Repository: `website`、Private）で管理する
- ブランチは`main`（公開用）と`develop`（通常作業用）の2本を運用する。詳細は「9. GitHub運用方針」を参照
- ホスティングはVercel（管理アカウント名義のTeamで管理する想定）。`main`ブランチへの反映がProduction公開に、`develop`ブランチへのpushはPreview環境（確認用URL）に、それぞれ自動的に反映される構成にする

**環境変数一覧（コードは対応済み。実際の値はSanityプロジェクト作成後に設定）**
- `NEXT_PUBLIC_SANITY_PROJECT_ID` — SanityプロジェクトID
- `NEXT_PUBLIC_SANITY_DATASET` — 使用するデータセット名（例：production）
- `NEXT_PUBLIC_SANITY_API_VERSION` — APIバージョン(日付形式。通常は変更不要)
- `SANITY_API_TOKEN` — 初期データ投入(`npm run seed`。10.5参照)など書き込みが必要な場合のみ設定する。普段は未設定のままでよい
- 本番環境ではVercelのプロジェクト設定画面（ブラウザ）で設定し、コードやGitHubには直接書き込まない。ローカル開発では`website/.env.local`（Gitには含まれない）に設定する。値の取得方法は`SANITY_SETUP_GUIDE.md`参照
- 万一これらの値が未設定でも、公開サイト側（Homeページ等）は自然なメッセージを表示するだけでエラー画面にはならない設計になっている（影響を受けるのは`/studio`のみ）

**フォルダ構成（現時点）**
```
website/
├── app/                 各ページ(App Router)
│   ├── studio/           Sanity Studio(管理画面)を埋め込むルート
│   └── error.tsx         予期しない不具合時のフォールバック画面
├── components/           再利用コンポーネント(Header, Hero, EventCardなど)
├── content/              更新頻度の低い部分(Hero/About紹介/Join紹介/Footer)のテキスト
├── sanity/
│   ├── schemaTypes/       イベント・お知らせ・ギャラリーアルバムのスキーマ定義
│   ├── lib/               クライアント・画像URL生成・GROQクエリ・日付/タグ変換
│   └── env.ts             環境変数の読み込み(1箇所に集約)
├── sanity.config.ts       Sanity Studioの設定
├── scripts/
│   └── seed.mjs           初期データ(Seed)投入スクリプト(10.5参照)
├── public/                静的ファイル
├── tailwind.config.ts     デザイントークン定義
├── CLAUDE.md              AIエージェント向けのプロジェクト指示書
└── package.json
```

**バックアップ方法**
- コード本体：GitHubにpushしていれば、それ自体が全履歴付きのバックアップになる
- Sanityのコンテンツ：年1回程度、開発者に`sanity dataset export`コマンドでのバックアップ書き出しを依頼し、管理アカウントのGoogle Driveに保存する
- 担当者：Administrator権限を持つ理事が、依頼のタイミングを管理する

---

# 9. GitHub運用方針

コードの保管場所であるGitHubについても、Sanityと同じ「個人ではなく団体として管理する」という考え方で運用する。

**Organization / Repository**
- Organization：スラッグ`la-kansai-club` / 表示名`LA Kansai Club`（Sanity・管理用Googleアカウントと名称を統一し、どのサービスも同じ名前で認識できるようにする）
- Repository：`website`（Private）。外部への公開や貢献募集を目的としないため非公開とする

**Owner権限の構成**
- Owner（メンバー管理・組織削除・請求情報変更ができる最上位の権限）は、**役職ではなく「その時点でIT運営を担っている運用責任者」を基準に付与する**
  - クラブ管理用GitHubアカウント（自動的にOwner）
  - その時点でIT運営を担当している理事（副会長・IT担当理事など、呼び方は年度によって変わってよい）1名程度
  - 「会長は自動的にOwner」のような役職固定の運用にはしない。IT運営の担当が交代したら、その都度Ownerを付け替える
- 実際にコードを書く開発者には、Organization全体のOwnerではなく、`website`リポジトリ限定のWrite権限を付与する
- ほとんどの理事はGitHubアカウント自体が不要（日常の更新はSanityの管理画面のみで完結するため）。GitHubへのアクセスは実際にコードを触る人だけに限定する

**ブランチ運用（事故防止のため2本構成）**
- `main`：公開されているサイトの状態を表すブランチ。Vercelの本番環境(Production)はこのブランチと連動させる
- `develop`：通常の作業はすべてこのブランチに対して行う
- 変更を公開するときだけ、`develop`の内容を`main`に反映する（マージする）。うっかりmainへ直接pushして即座に公開されてしまう事故を防ぐのが目的
- `develop`ブランチへのpushはVercelのPreview環境（確認用の別URL）に自動反映される想定のため、公開前に見た目や動作を確認してから`main`に反映できる
- ブランチは`main`と`develop`の2本のみとし、それ以上複雑な運用（機能ごとのブランチ分岐など）はこの規模では行わない

**役員交代に強い運用**
- 招待→動作確認→旧アカウント削除の3ステップは、Sanity（2.参照）と同じ手順をGitHubにも適用する
- 年1回のアクセス点検（2.参照）のタイミングで、GitHub Organizationのメンバーと権限も一緒に確認する
- Organizationの設定で「メンバー全員に2段階認証を必須にする」をオンにする
- 常時Write権限を持つのは1〜2名程度を目安にし、それ以上に増やさない

---

# 10. Sanity Studio運用ガイド

## 10.1 StudioのURL

| 環境 | URL | 用途 |
|---|---|---|
| 確認用(Preview) | `https://website-git-develop-la-kansai-club.vercel.app/studio` | 公開前の下書き確認・テスト用 |
| 本番(Production) | `https://website-gamma-nine-tyeg177uuh.vercel.app/studio` | 実際の会員・訪問者が見るサイトの内容を編集 |

日常の更新作業は**本番URL**で行います。確認用URLは、開発者が変更を加えた際の事前確認にのみ使用します。

## 10.2 ログイン方法

1. 上記いずれかのURLを開く
2. 「Continue with Google」を選ぶ
3. **理事本人の個人Googleアカウント**でログインする(事前にSanityのメンバーとして招待されている必要があります。招待されていない場合は10.4を参照)

## 10.3 理事の権限(2段階)

- **Administrator**：メンバーの追加・削除ができる。会長・副会長など1〜2名のみに限定
- **Editor**：記事・ページの作成・編集・公開ができるが、メンバー管理はできない。実際に更新作業をする理事に付与
- 権限の考え方の詳細は「4. 編集者の追加・削除方法」を参照

## 10.4 新しい理事を招待する手順

1. Administrator権限を持つ理事が[sanity.io/manage](https://www.sanity.io/manage)にログインする
2. プロジェクト(`gpi6bjc8`)を開き、「Members」タブを開く
3. 「Invite members」から、招待したい理事のメールアドレスを入力する
4. 権限(Administrator/Editor)を選んで送信する
5. 招待された理事は、届いたメールのリンクから参加を承認し、自分のGoogleアカウントでログインする
6. 招待した側は、実際にログインでき、試しに1件編集・公開できることを確認する

## 10.5 初期データ(Seed)の再実行方法

CMSをリセットした場合や新しい環境を作った場合に、サイトに表示されている基本内容(Home/About/Join/Donate/Contact/サイト設定)を初期データとして投入できます。`website/scripts/seed.mjs`で実装済みです。

1. Editor権限以上のAPIトークンを発行する(10.6参照)
2. `website/.env.local`に`SANITY_API_TOKEN=`を設定する
3. `website`フォルダで`npm run seed`を実行する
4. 各ドキュメントについて「作成しました」または「既に存在するためスキップしました」とログが表示される
5. **既存データがある項目は上書きされない設計**のため、何度でも安全に再実行できる
6. 実行後は10.6の手順でトークンを削除する

## 10.6 APIトークンの発行・削除方法

**発行**
1. [sanity.io/manage](https://www.sanity.io/manage) → プロジェクト(`gpi6bjc8`) → 「API」タブ → 「Tokens」→「Add API token」
2. 名前は任意。権限は用途に応じて選ぶ(Seed実行など書き込みが必要な場合は「Editor」、閲覧のみなら「Viewer」)
3. 表示されたトークンをコピーする(**この画面を閉じると二度と表示されません**)

**削除**
1. 同じ「Tokens」画面で、該当トークンの右側の削除ボタンをクリックする
2. 併せて`.env.local`から該当行を削除する

APIトークンは強い権限を持つため、使い終わったら都度削除することを推奨します。

## 10.7 develop → main → Production への公開手順

1. 開発者が`develop`ブランチに変更をpushすると、確認用URL(Preview)に自動反映される
2. 確認用URLで表示・動作を確認する
3. 問題がなければ、開発者が`develop`の内容を`main`にマージする
4. `main`への反映と同時に、Vercelが自動的に本番(Production)へデプロイする
5. 本番URLで最終確認する

技術的な詳細は`DEPLOYMENT_GUIDE.md`を参照。

## 10.8 CORS Originの追加方法

新しいドメイン(本番URLや今後の独自ドメインなど)からSanityへアクセスできるようにするには、CORS設定への追加が必要です。

1. [sanity.io/manage](https://www.sanity.io/manage) → プロジェクト(`gpi6bjc8`) → 「API」タブ → 「CORS Origins」
2. 「Add CORS origin」をクリック
3. 追加したいURL(例: `https://website-gamma-nine-tyeg177uuh.vercel.app`)を入力する
4. 「Allow credentials」に**チェックを入れて**保存する

これを忘れると、該当URLの`/studio`にアクセスはできても保存(Publish)ができない、といった不具合が起きます。

## 10.9 トラブルシューティング

| 症状 | 原因の可能性 | 対応 |
|---|---|---|
| ログイン画面から進まない | Sanityのメンバーとして招待されていない、または個人アカウント以外でログインしようとしている | 10.4を参照して招待状況を確認する |
| Studioを開くと`'id' is required for lists`というエラーが出る | Structure Builder(`sanity.config.ts`)のリスト定義がタイトル(日本語)からidを自動生成できていない(2026-07-30に実際に発生し、`.id()`を明示する形で修正済みの事例) | 開発者に連絡する。`sanity.config.ts`内のすべての`S.list()`/`S.listItem()`に英数字の`.id()`が設定されているか確認してもらう |
| 保存(Publish)できない | 現在アクセスしているURLがCORS Originsに登録されていない | 10.8の手順で追加する |
| 特定のページだけ「準備中です」と表示される | そのページのSanityドキュメントにまだ内容が入力されていない(異常ではない) | Studioで該当ページを開き、内容を入力してPublishする |

上記で解決しない場合は、開発者にこのドキュメントの該当項目番号と、実際に表示されたエラー内容を伝えてください。

---

# 参照ドキュメント

- デザインの基準：`DESIGN_RULES.md`
- 新しい開発者向けの引き継ぎガイド：`README_FOR_NEW_DEVELOPER.md`
- AIエージェント（Claude Code等）向けの指示：`CLAUDE.md`
- Sanityプロジェクトの初回作成手順（非エンジニア向け）：`SANITY_SETUP_GUIDE.md`
- Vercel本番デプロイ手順：`DEPLOYMENT_GUIDE.md`
